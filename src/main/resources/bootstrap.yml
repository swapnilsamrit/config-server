spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/swapnilsamrit/TestGit
          search-paths: "app*"
          #clone-on-start: true
      fail-fast: true
server:
  port: 8888